{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "build/server/fetch-handler.js"
    },
    {
      "src": "/(.*)",
      "dest": "build/client/$1"
    }
  ]
}
